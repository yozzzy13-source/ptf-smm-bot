PTF SMM Bot v0.3.2 — Service/Admin Commands

Added Telegram admin commands:
- /commands, /help, /start
- /status, /health, /ping
- /models
- /today
- /upcoming
- /schedule
- /setup
- /clear_history, /reset_context
- /restart (disabled by default, enable with ENABLE_TELEGRAM_RESTART_COMMAND=true)
- /image_on, /image_off
