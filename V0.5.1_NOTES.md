# v0.5.1 Startup Fix

Fixes Railway startup crash:
- Adds missing `visualNextStepKeyboard` export in `src/services/telegramKeyboardService.js`.
- Keeps v0.5.0 state-aware router logic intact.

Deploy this version over v0.5.0, then test `/status`, `/campaign_state`, and a visual-only request.
