PTF SMM Bot v0.3.3

Fix: some newer reasoning models do not accept custom temperature values.

Changes:
- openaiService now omits temperature for gpt-5*, o1*, o3*, o4* models.
- Adds model/schema/temperature logging around OpenAI JSON calls.
- Keeps temperature for older chat models such as gpt-4.1-mini.
