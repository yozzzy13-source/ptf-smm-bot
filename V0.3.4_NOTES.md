PTF SMM Bot v0.3.4 — Error visibility + Telegram HTML safety

- Telegram sendMessage now retries failed HTML messages as plain text.
- Prevents the whole workflow from failing when Telegram rejects malformed/truncated HTML.
- OpenAI errors now include status and response body details in logs and user error message.
- OpenAI callJsonAgent now has three fallbacks: json_schema -> json_object -> plain JSON prompt.
- Health version updated to 0.3.4.
